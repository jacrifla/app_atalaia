import 'package:flutter/material.dart';

// Constantes para as cores alternadas

const Color activeColor = Color.fromARGB(255, 196, 196, 12);
const Color inactiveColor = Color.fromARGB(255, 141, 141, 141);
